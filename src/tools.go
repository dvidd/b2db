package tools

import "fmt"


func read() (index int) {
	
    fmt.Println(index)
    for i := 0; i < 10; i++ {
        fmt.Println(index)
    }
    return

}

func add() (index int) {
    return
}

func delete() (index int) {
    return
}

